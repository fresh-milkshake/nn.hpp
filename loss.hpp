#ifndef LOSS_HPP
#define LOSS_HPP

float dumb_L(float W[], float X[], float Y[]) {
  float result = 0.0f;
  size_t data_size = sizeof(X);
  for (size_t i = 0; i < data_size; i++) {
    float dis = 0.0f;
    result += dis;
  }
  return result;
}

#endif // LOSS_HPPLOSS_HPP
